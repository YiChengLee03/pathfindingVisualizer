import { AlgorithmContainer, Header, AlgorithmType } from './algorithm.styles';

const Algorithm = () => {
  return (
    <AlgorithmContainer>
      <Header>Algorithm </Header>
      <AlgorithmType>Djikstra's Algorithm</AlgorithmType>
    </AlgorithmContainer>
  );
};

export default Algorithm;
