import { styled } from 'styled-components';

export const AlgorithmContainer = styled.div`
  flex: content;
`;

export const Header = styled.h3`
  text-transform: uppercase;
`;

export const AlgorithmType = styled.h4`
  font-size: small;
`;
