import { NodesContainer, Header, NodeType } from './nodes.styles';

const Nodes = () => {
  return (
    <NodesContainer>
      <Header>Nodes</Header>
      <NodeType>Regular</NodeType>
    </NodesContainer>
  );
};

export default Nodes;
