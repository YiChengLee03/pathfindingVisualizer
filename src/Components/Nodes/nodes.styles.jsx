import { styled } from 'styled-components';

export const NodesContainer = styled.div`
  flex: content;
`;

export const Header = styled.h3`
  text-transform: uppercase;
`;

export const NodeType = styled.h4`
  font-size: small;
`;
